<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ludan.app.mapper.KbQaPairMapper">

    <select id="findList" resultType="com.ludan.app.entity.KbQaPair">
        SELECT * FROM kb_qa_pair
        <where>
            del_flag = 0
            <if test="p.kbId != null">
                AND kb_id = #{p.kbId}
            </if>
            <if test="p.keyword != null and p.keyword != ''">
                AND (question LIKE CONCAT('%', #{p.keyword}, '%') OR answer LIKE CONCAT('%', #{p.keyword}, '%'))
            </if>
            <if test="p.auditStatus != null and p.auditStatus != ''">
                AND audit_status = #{p.auditStatus}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

</mapper>
