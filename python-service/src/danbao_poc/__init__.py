"""Danbao RAG and graph POC package."""

__version__ = "0.1.0"

# 部署版本标记：每次改动解析/检索逻辑后递增，用于确认 dev 环境是否已部署新代码。
# 通过 GET /health 返回的 build_tag 校验。
BUILD_TAG = "20260813-interrogative-anchor-procedure-boost-v7"
